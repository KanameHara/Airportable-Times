import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function index() {
  return (
    <div>
      initial page
      {/* ログイン画面のページをここに配置 */}
    </div>
  );
}
